version: 1.0
runtime: python311
build:
  commands:
    build:        
      - pip3 install -r requirements.txt
run:
  runtime-version: 3.11
  pre-run:  
    - pip3 install -r requirements.txt
  command: python3 bot.py
environment:
  variables:
    open_ai_key: {{ secrets_manager:arn:aws:secretsmanager:us-east-1:058264240189:secret:CSOK_POLICE_SECRETS-CUBdKB:open_ai_key }}
    discord_token: {{ secrets_manager:arn:aws:secretsmanager:us-east-1:058264240189:secret:CSOK_POLICE_SECRETS-CUBdKB:discord_token }}
    